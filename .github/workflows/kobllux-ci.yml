# KOBLLUX NODE.FIELDS — CI (LEGACY · desativado)
# Triggers automáticos removidos para limpar runs presos
# CI ativo: .github/workflows/kobllux-pulse.yml
# Verbum Codex KBLX hash: f544e7482b2c8426

name: KOBLLUX CI (legacy)

on:
  workflow_dispatch:
    inputs:
      note:
        description: "Este workflow foi migrado para kobllux-pulse.yml"
        required: false
        default: "use kobllux-pulse.yml"

jobs:
  migrated:
    name: "CI migrado para kobllux-pulse.yml"
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Este workflow foi desativado."
          echo "O CI ativo está em: .github/workflows/kobllux-pulse.yml"
          echo "KOBLLUX VIVO ∆³"
